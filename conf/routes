# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET /login.htm                          controllers.Application.login()
GET /logout.htm                         controllers.Application.logout()
POST /authenticate.htm                  controllers.Application.authenticate()
GET /registration.htm                   controllers.Registration.registration()
POST /registration_finish.htm           controllers.Registration.registrationFinish()
GET /my_dashboard.htm                   controllers.Dashboard.index()
GET /my_profile.htm                     controllers.Dashboard.myProfile()